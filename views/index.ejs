<% include ../views/partials/header %>
  <form method="post" action="/todo/add">
    <div>
        <label> Todo Name </label>
        <input type="text" name="text" placeholder="Add Todo" value="">
    </div>
    <div>
        <label> Todo Body  </label>
        <textarea name="body" rows="5" cols="80" placeholder="Add Body "></textarea>
    </div>
    <button type="submit" name="button"> Submit</button>
  </form>
  <ul>
    <% todos.forEach(function(todo) { %>
      <li> <%= todo.text %> - <%= todo.body %>
        <a data-id="<%= todo._id %>" href="#" class="delete-todo"> Delete</a>
        <a href="/todo/edit/<%= todo._id%> "> Edit </a>
      </li>
    <% }); %>
  </ul>
<% include ../views/partials/footer %>
